export const metadata = {
  title: "Login — SMM Panel",
  description: "Login to your SMM Panel account to access social media marketing services.",
};

export default function AuthLayout({ children }) {
  return <>{children}</>;
}
